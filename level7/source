char c[200];

void m(void) {
  time_t t;

  t = time(0);
  printf("%s - %d\n", c, (int)t);
}

int main(int argc, char **argv) {
  char *s1;
  char *s2;
  FILE *pass;

  s1 = malloc(8);
  *s1 = 1;

  s2 = malloc(8);
  *s2 = 1;

  strcpy(s1[1], argv[1]);
  strcpy(s2[1], argv[2]);

  pass = fopen("/home/user/level8/.pass","r");

  fgets(c, 68, pass);
  puts("~~");

  return 0;
}